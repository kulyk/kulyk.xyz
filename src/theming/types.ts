export type Theme = {
  background: {
    main: string;
    hover: string;
  };
  text: {
    main: string;
    secondary: string;
  };
  button: {
    text: string;
    background: string;
    hover: string;
  };
  input: {
    text: string;
    background: string;
    border: string;
  };
  link: {
    default: string;
    hover: string;
  };
  code: {
    background: string;
    text: string;
  };
  icon: {
    background: string;
  };
  card: {
    background: string;
  };
  success: string;
  successText: string;
  error: string;
  errorText: string;
};
