export type Theme = {
  background: {
    main: string;
    hover: string;
  };
  text: {
    main: string;
    secondary: string;
  };
  button: {
    text: string;
    background: string;
    hover: string;
  };
  link: {
    default: string;
    hover: string;
  };
  code: {
    background: string;
    text: string;
  };
};
