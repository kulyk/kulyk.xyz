export type Theme = {
  background: string;
  text: {
    main: string;
    secondary: string;
  };
  button: {
    text: string;
    background: string;
    hover: string;
  };
  link: {
    default: string;
    hover: string;
  };
};
